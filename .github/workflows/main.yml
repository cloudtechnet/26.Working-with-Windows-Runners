name: Windows Runner Demo

on: push

jobs:
  demo:
    runs-on: windows-latest

    
    steps:
      - name: PowerShell Example
        shell: cmd
        run: |
          echo running CMD
          dir
