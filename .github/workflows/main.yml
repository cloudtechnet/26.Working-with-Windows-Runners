name: Windows Runner Demo

on: push

jobs:
  demo:
    runs-on: windows-latest

    
    steps:
      - name: PowerShell Example
        run: |
          Write-Host "Running PowerShell"
          Get-Date
