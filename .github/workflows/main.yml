name: Windows Runner Demo

on: push

jobs:
  demo:
    runs-on: windows-latest

    
    steps:
      - name: PowerShell Example
        shell: bash
        run: |
          echo running bash
          pwd
