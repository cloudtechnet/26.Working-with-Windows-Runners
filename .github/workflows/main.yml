name: Checkout Example

on: push

jobs:
  checkout-demo:
    runs-on: windows-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: List Files
        run: dir
